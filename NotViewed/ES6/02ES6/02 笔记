函数
    默认值
        1、当不传参数的值或者是undefined时会使用默认值
        2、使用参数默认值时建议把有默认的参数放到参数列表的后面
        3、参数默认值是函数调用时取值（惰性求值）
    length属性：返回从左往右没有遇到参数有默认值的参数个数（不包含rest参数）
    name属性：返回函数的函数名（匿名函数（anonymous）、有名函数（函数名（优先）或者变量名）、绑定函数（bound 函数名））

    剩余运算符（rest）
        1、无论是解构还是参数都须放在最后
        2、由于返回是数组，可以用于代替arguments对象
